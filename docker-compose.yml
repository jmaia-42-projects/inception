version: "3"
services:
  nginx:
    build: ./nginx
    ports:
      - "80:80"
    volumes:
      - wordpress-data:/srv
  wordpress:
    build: ./wordpress
    volumes:
      - wordpress-data:/srv
      #  mariadb:
      #    build: ./mariadb
volumes:
  wordpress-data:
